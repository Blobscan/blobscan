---
"@blobscan/web": minor
"@blobscan/docs": patch
---

Add a configurable announcement banner to the app layout, controlled via `PUBLIC_BANNER_TEXT`, `PUBLIC_BANNER_LINK_TEXT`, and `PUBLIC_BANNER_LINK_URL` env vars

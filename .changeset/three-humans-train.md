---
"@blobscan/api": minor
"@blobscan/web": patch
---

Restructured the overall stats response. The `category` and `rollup` fields are now grouped under a `dimension` object, distinct from `metrics` and `updatedAt`

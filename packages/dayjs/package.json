{
  "name": "@blobscan/dayjs",
  "description": "Blobscan's Day.js shared library",
  "private": true,
  "version": "0.0.1",
  "main": "./index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint .",
    "lint:fix": "pnpm lint --fix",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "dayjs": "^1.11.7"
  },
  "eslintConfig": {
    "root": true,
    "extends": [
      "@blobscan/eslint-config/base"
    ]
  }
}
